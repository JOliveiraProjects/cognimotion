# CognitiveMotionIntelligence — Python Server Dependencies
# Install: pip install -r requirements.txt

# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.2.0
numpy>=1.26.0

# ── LLM (GPT-2 e variantes) ───────────────────────────────────────────────────
transformers>=4.40.0
tokenizers>=0.19.0
# Acelera geração de texto no GPU:
# accelerate>=0.29.0  # descomente se usar CUDA

# ── Busca vetorial (opcional mas recomendado para MotionMemoryBank) ───────────
# CPU-only:
faiss-cpu>=1.8.0
# GPU (descomente se tiver CUDA):
# faiss-gpu>=1.8.0

# ── Async / networking ────────────────────────────────────────────────────────
# asyncio faz parte da stdlib do Python 3.7+

# ── Utilitários ───────────────────────────────────────────────────────────────
scipy>=1.12.0        # usado por métricas de qualidade (pose_legality_validator)
